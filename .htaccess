RewriteEngine on
RewriteCond %{SERVER_PORT} 80
RewriteRule ^(.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

AuthName "Page d'administration protégée"
AuthType Basic
AuthUserFile "/home/etnszqj/httpdocs/.htpasswd"
Require valid-user